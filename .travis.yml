language: python

python:
  - "2.7"

os:
  - linux
  - osx

# command to install dependencies
install:
  - "pip install --only-binary=numpy,scipy numpy scipy"
  - "pip install ."

# command to run tests
script: pytest
